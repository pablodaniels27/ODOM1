<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns="http://javafx.com/javafx/17.0.2-ea" xmlns:fx="http://javafx.com/fxml/1" fx:controller="controllers.InicioController">
    <!-- Cuadrante 1: Lista de empleados -->
    <left>
        <VBox fx:id="employeeList" spacing="10" alignment="TOP_LEFT" styleClass="employee-section">
            <Label text="Lista de empleados" styleClass="header-label"/>
            <ListView fx:id="employeeListView" styleClass="employee-list"/>
        </VBox>
    </left>

    <!-- Cuadrante 2: Calendario -->
    <center>
        <VBox fx:id="calendarSection" spacing="10" alignment="TOP_CENTER" styleClass="calendar-section">
            <Label text="Calendario" styleClass="header-label"/>
            <DatePicker fx:id="calendarPicker"/>
        </VBox>
    </center>

    <!-- Cuadrante 3: Asistencias semanales -->
    <right>
        <VBox fx:id="attendanceSection" spacing="10" alignment="TOP_RIGHT" styleClass="attendance-section">
            <Label text="Asistencias semanales" styleClass="header-label"/>
            <TableView fx:id="attendanceTable" styleClass="attendance-table">
                <columns>
                    <TableColumn text="Día" fx:id="dayColumn"/>
                    <TableColumn text="Horas" fx:id="hoursColumn"/>
                </columns>
            </TableView>
        </VBox>
    </right>
</BorderPane>